import React, { useState } from "react";
import { Grommet, Box, Heading, Paragraph, Text } from "grommet";

const TopBar = (props) => {
  return (
    <>
      <div className="app-topbar-main-cont">
        <div className="topbar-page-lab-cont"></div>
      </div>
    </>
  );
};

export default TopBar;
